[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pathmnist_mlops"
version = "0.0.1"
description = "End-to-end MLOps workflow for PathMNIST classification with experiment tracking, hyperparameter optimization, and deployment."
authors = [
  { name = "LSP Team", email = "your@email.com" },
]
license = { file = "LICENSE" }
keywords = ["machine learning", "MLOps"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
]
readme = "README.md"
requires-python = ">=3.12"
dynamic = ["dependencies", "optional-dependencies"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.setuptools.dynamic.optional-dependencies]
dev = {file = ['requirements_dev.txt']}

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["I", "N", "E", "W", ]

[tool.coverage.run]
omit = ["tests/*"]

[tool.mypy]

[[tool.mypy.overrides]]
module = ["sklearn.*"]
ignore_missing_imports = true
