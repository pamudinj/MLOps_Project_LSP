name: Unit Tests & Build Container

on:

  push:
    branches:
      - main

    paths:
      - "dockerfiles/drift_api.dockerfile"
      - "cloudbuild_drift_api.yaml"
      - ".github/workflows/build_drift_api.yaml"


jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v5

      - name: Auth with GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json:
            ${{ secrets.GCLOUD_SERVICE_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Submit cloud build
        run: |
          gcloud builds submit \
            . \
            --config \
            cloudbuild_drift_api.yaml \
            --async
